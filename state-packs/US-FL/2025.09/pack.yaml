state: US-FL
version: "2025.09"
title: "Florida General Education Core & SUS Requirements"
effective_from: "2025-08-01"
description: "Florida State University System general education requirements and common prerequisite coursework for AA to BA transfer."

datasets:
  gen_ed_areas: "datasets/gen_ed_areas.yaml"
  gordon_rule: "datasets/gordon_rule.yaml"

rules:
  program:
    - "rules/program.yaml"
  course:
    - "rules/course.yaml"
  termPlan:
    - "rules/termplan.yaml"

references:
  citations: "references/citations.json"
