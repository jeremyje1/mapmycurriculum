{
  "name": "curriculum-map-tool",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": "22.x"
  },
  "packageManager": "npm@10.8.1",
  "scripts": {
    "build": "npm --prefix apps/web run build",
    "dev": "npm --prefix apps/web run dev",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "stripe:verify": "tsx scripts/stripe-verify.ts",
    "stripe:setup:marketing": "tsx scripts/stripe-products-marketing.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "stripe": "14.25.0"
  },
  "devDependencies": {
    "cypress": "^13.12.0",
    "tsx": "4.7.0",
    "typescript": "^5.9.2"
  }
}
