{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "pnpm --filter curriculum-map-web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "crons": [
    {
      "path": "/api/health",
      "schedule": "*/5 * * * *"
    }
  ]
}
